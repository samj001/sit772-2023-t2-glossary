<!DOCTYPE html>
<html>
<head>
    <style>
        table,td,th{border:1px solid black}
    </style>
</head>

<body>
    <h1><%= title %></h1>
    <p>Welcome to <%= subheading %></p>
    <table>
        <% items.forEach((item) => { %>
            <tr>
                <td>
                    <%= item.id %>
                </td>

                <td>
                    <%= item.term %>
                </td>

                <td>
                    <%= item.description %>
                </td>

                <td>
                    <%= item.reference %>
                </td>


            </tr>
        
        <% }); %>

        
    </table>

</body>
</html>